KEYCHAINOS FEATURE UPDATE 0.2.0

The ZIP is GitHub-ready. Extract it over the local KeychainOS Git clone.
It contains firmware/ source folders, flat update/ payloads, update/update.json,
and android/publish_keychainos.py.

ESP workflow: Settings > Update. Git pushes to GitHub; the ESP pulls from GitHub.
RGB format: 240 x 320, big-endian RGB565, exactly 153600 bytes, stored in /media/images.
