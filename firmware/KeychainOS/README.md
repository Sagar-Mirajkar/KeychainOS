# Main Firmware

The main Arduino sketch must use the known-good TFT_eSPI configuration copied into `config/User_Setup.h`.

Do not add multiple peripherals until each diagnostic sketch passes independently.
